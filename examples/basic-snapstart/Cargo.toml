[package]
name = "basic-snapstart"
version = "0.1.0"
edition = "2021"

[dependencies]
lambda_runtime = { path = "../../lambda-runtime" }
serde = "1.0.219"
serde_json = "1.0"
tokio = { version = "1", features = ["macros"] }
